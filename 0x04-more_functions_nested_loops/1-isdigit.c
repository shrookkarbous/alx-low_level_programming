#include "main.h"

/**
 * _isdigit - a function that checks for a digit (0 through 9)
 *
 * @c: takes the inputs
 *
 * Return:  1 if c is a digit
 *		0 otherwise
*/

int _isdigit(int c)
{
	if (C >= 48 && C <= 57)
		return (1);
	else
		return (0);
}
