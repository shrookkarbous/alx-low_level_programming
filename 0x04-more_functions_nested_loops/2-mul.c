#include "main.h"

/**
 * mul -i a function that multiplies two integers
 *
 * @a: takes first inputs
 * @b: takes second inputs
 *
 * Return: Always return (result)
*/

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
